{
  "name": "calisthenics-ascension",
  "version": "1.0.0",
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "vercel-build": "npm install && npm run build",
    "postinstall": "npm rebuild --update-binary"
  }
}
